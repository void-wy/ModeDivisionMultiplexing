/*
///////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////////

#include "SimulatedAnnealing.h"

///////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////////

void main()
{
	SLM *slm = new SLM(400, 400);	//创建控制SLM硬件的对象
	//CCD *ccd = new CCD();	//创建控制CCD硬件的对象
	ImageProcessing *IP = new ImageProcessing();	//创建包含IP方法的对象
	SimulatedAnnealing *SA = new SimulatedAnnealing(slm, ccd, IP);	//创建SA的对象

	SA->setModeIdeal("LP11a", 15);
	SA->setParameterSA(10000000, 100, 0.95, 100);
	SA->run(200, 200);

	delete SA;	//释放SA的对象
	delete IP;	//释放包含IP方法的对象
	//delete ccd;	//释放控制CCD硬件的对象
	delete slm;	//释放控制SLM硬件的对象
}

///////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////////
*/